export const placeholder = (w = 1600, h = 900, label = '') => { const svg = `<svg xmlns='http://www.w3.org/2000/svg' width='${w}' height='${h}'><rect width='100%' height='100%' fill='#e5e7eb'/><text x='50%' y='50%' font-family='sans-serif' font-size='${Math.max(12, w / 40)}' fill='#9ca3af' text-anchor='middle' dominant-baseline='middle'>${label || `${w}×${h}`}</text></svg>`; return `data:image/svg+xml,${encodeURIComponent(svg)}`; };
